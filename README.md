export telegram chat as JSON (telegram desktop)

add result.json and voice_messages in this project folder

run ogg_to_wav_to_txt.py
edit days_per_period if u need to.

poetry run python3 src/main.py
